{
  "name": "nuclear-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "NuClear: clinical-grade scientific figure authoring workstation for nuclear medicine",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -b",
    "test": "node --test",
    "build": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
